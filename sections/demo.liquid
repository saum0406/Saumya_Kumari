<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Gift Guide</title>
  
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Jost:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    *,*::before,*::after{ box-sizing:border-box; }

    body {
      margin: 0;
      font-family: 'Jost', system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      background: #fff;
      color:#000;
    }

    /* ---- Header (Top Strip) ---- */
    .masthead{ background:#f5f5f5; }
    .masthead__inner{
      max-width:1440px; margin:0 auto; padding:10px 24px;
      display:grid; grid-template-columns:1fr auto 1fr;
      align-items:center; gap:16px;
    }
    .brand{ font-weight:600; }
    .tagline{ text-align:center; font-size:14px; opacity:.9; }
    .choose{
      justify-self:end; background:#FFD83D; padding:10px 16px;
      text-decoration:none; color:#000; font-weight:500;
      border:1px solid #e0c200;
    }

    /* ---- Hero Section ---- */
    .hero{
      position:relative;
      width:100%;
      min-height:720px;
      background:url("{{ 'bgimg.jpg' | asset_url }}") center/cover no-repeat;
    }
    .hero-content{
      position:absolute;
      inset:auto auto 80px 60px; /* bottom-left corner */
      color:#000;
      max-width:520px;
    }
    .hero h1{
      font-size:clamp(42px,6vw,88px);
      font-weight:500;
      letter-spacing:-.02em;
      line-height:.98;
      margin:0;
    }
    .hero p{
      font-size:18px;
      line-height:1.45;
      margin:24px 0 0;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      gap:12px;
      margin-top:32px;
      background:#000;
      color:#fff;
      padding:12px 24px;
      text-transform:uppercase;
      letter-spacing:.06em;
      text-decoration:none;
      font-size:14px;
      transition:background .2s ease;
    }
    .btn:hover{ background:#222; }
    .btn .arrow{ transition:transform .2s ease; }
    .btn:hover .arrow{ transform:translateX(4px); }

    /* ---- Bottom Info Bar ---- */
    .info-bar{ background:#F5F5F5; padding:8px 0; }
    .info-bar .container{
      max-width:1440px;
      margin:0 auto;
      padding:0 24px;
      text-align:center;
    }
    .info-bar p{
      margin:0;
      font-size:12px;
      letter-spacing:.08em;
      text-transform:uppercase;
    }

    /* ---- Mobile adjustments ---- */
    @media (max-width: 768px){
      .hero-content{ inset:auto auto 32px 20px; }
      .hero p{ max-width:90vw; font-size:16px; }
      .masthead__inner{ grid-template-columns:1fr; text-align:center; }
      .choose{ justify-self:center; margin-top:8px; }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header class="masthead">
    <div class="masthead__inner">
      <div class="brand">TISSO VISON</div>
      <div class="tagline">Find the ideal gift for your loved ones.</div>
      <a class="choose" href="#" aria-label="Choose gift">CHOOSE GIFT →</a>
    </div>
  </header>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-content">
      <h1>The Gift Guide</h1>
      <p>
        Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season.
      </p>
      <a href="#" class="btn" aria-label="Shop now">SHOP NOW <span class="arrow">→</span></a>
    </div>
  </section>

  <!-- Info Bar -->
  <section class="info-bar">
    <div class="container">
      <p>SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL</p>
    </div>
  </section>

</body>
</html>
